{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true, // references 사용을 위한 필수 설정
    "declaration": true, // 빌드 시 타입 선언 파일 생성
    "declarationMap": true
  },
  "include": ["**/*"],
  "exclude": ["node_modules", "dist"]
}
